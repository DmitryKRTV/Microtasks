import React from 'react';

export type FooterType = {
    title: string
}

const Footer = (props: FooterType) => {
    return (
        <div>
            Footer
        </div>
    );
};

export default Footer;
