import React from 'react';

export type BodyType = {
    title: string
}

const Body = (props: BodyType) => {
    return (
        <div>
            Body
        </div>
    );
};

export default Body;
